list = [1,2,3]
IO.puts hd(list)
IO.puts tl(list)

# hd []

IO.puts [11, 12, 13]
IO.puts [104, 101, 108, 108, 111]	# Elixir sees printable ASCII numbers, print them as a charlist

# use i and h to inquire what the argument / function that comes after is
